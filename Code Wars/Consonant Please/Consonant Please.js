const sortLetters = arr => arr.reduce((accum, c) => 
  !curr.forEach(elem => /[aeiou]/i.test(elem) 
    ? accum[0].push(elem.toUpperCase()) 
    : !(/\d/.test(elem)) && accum[1].push(elem.toUpperCase())) && accum,[[], []]);